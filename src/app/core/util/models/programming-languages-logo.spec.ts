import { ProgrammingLanguagesLogo } from './programming-languages-logo';

describe('ProgrammingLanguagesLogo', () => {
  it('should create an instance', () => {
    expect(new ProgrammingLanguagesLogo()).toBeTruthy();
  });
});
