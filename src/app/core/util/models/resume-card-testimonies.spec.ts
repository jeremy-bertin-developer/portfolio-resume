import { ResumeCardTestimonies } from './resume-card-testimonies';

describe('ResumeCardTestimonies', () => {
  it('should create an instance', () => {
    expect(new ResumeCardTestimonies()).toBeTruthy();
  });
});
