import { ResumeCardStudies } from './resume-card-studies';

describe('ResumeCardStudies', () => {
  it('should create an instance', () => {
    expect(new ResumeCardStudies()).toBeTruthy();
  });
});
