import { ResumeCardCoursesTrainings } from './resume-card-courses-trainings';

describe('ResumeCardCoursesTrainings', () => {
  it('should create an instance', () => {
    expect(new ResumeCardCoursesTrainings()).toBeTruthy();
  });
});
