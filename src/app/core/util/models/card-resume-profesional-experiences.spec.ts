import { CardResumeProfesionalExperiences } from './card-resume-profesional-experiences';

describe('CardResumeProfesionalExperiences', () => {
  it('should create an instance', () => {
    expect(new CardResumeProfesionalExperiences()).toBeTruthy();
  });
});
